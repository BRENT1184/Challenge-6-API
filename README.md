# Challenge-6-API
The gitignore file is preventing the api_key.py file from being pushed to github, without the api_key.py file, the weather.py and vacation.py code will not run. So the code inside the gitignore was commented out, so as to allow the api_keys.py file to be pushed to github.
